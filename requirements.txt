# requirements.txt — SurgicalVLM_Robotics (TODO.md §9.2.6)
# Core ML / DL
torch>=2.1.0
torchvision>=0.15.2
transformers>=4.51.0,<5.0
accelerate>=0.30.0

# LoRA / PEFT
peft>=0.13.0

# Quantisation (4-bit via bitsandbytes)
bitsandbytes>=0.43.0

# Checkpoint serialisation
safetensors>=0.4.0

# Qwen2.5-VL processor helpers
qwen-vl-utils>=0.0.8

# Video / Image I/O
opencv-python-headless>=4.10.0
Pillow>=10.0.0

# Gradio UI
# NOTE: huggingface-hub 0.36.2 breaks gradio 6.24.0 (UI only) — pin huggingface-hub or gradio if the UI must run on that combination.
gradio>=5.22.0

# Visualisation (timeline plots in Gradio UI)
matplotlib>=3.8.0

# Metrics / Evaluation
scikit-learn>=1.3.0
evaluate>=0.4.0

# Data / Numerical
numpy>=1.24.0
pandas>=2.1.0

# Testing
pytest>=8.2.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.1
rich>=13.0.0